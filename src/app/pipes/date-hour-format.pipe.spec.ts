import { DateHourFormatPipe } from './date-hour-format.pipe';

describe('DateHourFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DateHourFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
